// credit: https://stackoverflow.com/questions/16637051/adding-space-between-numbers

const roundingUpPriceOrNumber = (num: number) => {
  return Math.round(Math.ceil(num))
}

export default roundingUpPriceOrNumber
